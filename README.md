# Markov

Simple implementation of Markov chains in typescript inspired from node-markovify.

## Usage

```bash
npm install
ts-node examples/text.ts
```

Add a file within `examples/data` folder containing your text corpus.
See `examples/text.ts`.
